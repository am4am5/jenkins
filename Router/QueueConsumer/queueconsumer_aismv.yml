apiVersion: apps/v1
kind: Deployment
metadata:
  name: aismv-router-queueconsumer
  namespace: aismv
  labels:
    app: aismv-router-queueconsumer
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: aismv-router-queueconsumer
  template:
    metadata:
      labels:
        app: aismv-router-queueconsumer
    spec:
      containers:
      - image: 172.16.9.49:30020/aismv/router/aismv-router-queueconsumer:$BUILD_NUMBER
        name: aismv-router-queueconsumer
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 120
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aismv-router-queueconsumer
  namespace: aismv
spec:
  type: NodePort
  selector:
    app: aismv-router-queueconsumer
  ports:
  - nodePort: 31107
    protocol: TCP
    port: 8080
    targetPort: 8080
